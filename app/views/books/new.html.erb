<h1>投稿フォーム</h1>
<%= form_with model: Book.new do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Body</h4>
  <%= f.text_area :body %>
  <h4>image</h4>
  <%= f.file_field :image, accept: "image/*" %>
  <%= f.submit 'Create Book' %>
<% end %>